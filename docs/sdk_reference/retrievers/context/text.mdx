---
title: Text
description: The Text Context Classes
---

## Text

```python
class Text(Context)
```

Text context class.

#### from\_file

```python
@classmethod
def from_file(cls,
              file: str | Path | IO,
              type: Literal["txt"] = "txt",
              params: dict = None) -> "Text"
```

Load a LongText instance from a file.

## TextFile

```python
class TextFile(Context)
```

Text document context class.

#### from\_file\_store

```python
@classmethod
def from_file_store(cls,
                    file_ref: str,
                    file_store: FileStore,
                    params: dict = None) -> "TextFile"
```

Load a TextFile instance from a file.

**Arguments**:

- `file` - The file to load.
- `file_store` - The file store to use.
- `type` - The type of file to load.
- `params` - The parameters to pass to the constructor.

